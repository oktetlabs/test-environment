basic
    chunk
    log(ts, lvl, entity, user, text)

spanning
    start
    end

parametrized
    args

named
    name
    objective
    authors

internal [basic]
    child
    add_child(child)
    del_child(child)

branching [internal]
    branches
    add_child(child)
    del_child(child)

root [internal]

package [branching, named, parametrized, spanning]

session [branching, parametrized, spanning]

test [basic, named, parametrized, spanning]
    tin
    page
    verdicts
